export type Submission = {
    submissionId: string;
    submissionDate: string;
    userSubId: string;
    firstName: string;
    lastName: string;
    email: string;
}

export type SubmissionPost = {
    userSubId: string;
    firstName: string;
    lastName: string;
    email: string;
}
